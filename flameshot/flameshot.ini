[General]
# Enable the Wayland-native capture path (grim adapter) to respect per-monitor scaling
useGrimAdapter=true
# Suppress the warning about using grim so the tray launch stays quiet
disabledGrimWarning=true
