# Workaround for WirePlumber segfault after update (libcamera monitor path)
# Keep video stack mostly intact while disabling only the crashing monitor.
wireplumber.profiles = {
  main = {
    monitor.libcamera = disabled
  }
}
