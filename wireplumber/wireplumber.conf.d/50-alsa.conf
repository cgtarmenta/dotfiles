# ALSA monitor defaults
# (migrated from the old wireplumber/main.lua.d/50-alsa.lua — WirePlumber 0.5
# dropped the Lua main.lua.d config path in favor of this conf.d format)
#
# Note: "flat-volumes" is a PipeWire-Pulse property, not a WirePlumber ALSA
# monitor one — it lives in pipewire/pipewire-pulse.conf.d/99-noflat.conf.
monitor.alsa.properties = {
  api.alsa.use-acp = true
  session.suspend-timeout-seconds = 0
}
